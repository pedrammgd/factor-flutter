import 'package:flutter/material.dart';

class FactorUnofficialList extends StatelessWidget {
  const FactorUnofficialList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('list'));
  }
}
