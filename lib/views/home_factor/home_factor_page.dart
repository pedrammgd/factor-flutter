import 'package:flutter/material.dart';

class HomeFactorPage extends StatelessWidget {
  const HomeFactorPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hello'),
      ),
    );
  }
}
